# actions/__init__.py

# This file marks the 'actions' directory as a Python package.
# You can also configure shared utilities or logging here if needed.

import logging

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(name)s: %(message)s"
)
